<?php

namespace App\Http\Controllers;

abstract class Controller
{
    // protected function respond($data, $status = 200)
    // {
    //     if (is_string($status) || $status == 0 || $status > 599)
    //         $status = 500;

    //         return response()->json([
    //             'status' => 'success',
    //             'message' =>
    //         ])
    // }
}
